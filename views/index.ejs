<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library Home</title>
    <link rel="stylesheet" href="/styles.css"> <!-- ✅ Ensures CSS is loaded -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"> <!-- ✅ Bootstrap for styling -->
</head>
<body>
    <%- include("navbar") %>

    <div class="container text-center">
        <!-- Welcome Section -->
        <h1>Welcome to the Library</h1>
        <p>Discover a world of books, explore weather updates, and manage your library seamlessly.</p>

        <hr>

        <!-- About the Search Options -->
        <section class="search-info">
            <h2>Explore Our Search Options</h2>
            <p>Choose the right tool for your needs:</p>

            <div class="row">
                <div class="col-md-4">
                    <div class="card mb-4">
                        <div class="card-body">
                            <h3 class="card-title">Gutenberg Books</h3>
                            <p class="card-text">
                                Access free eBooks from Project Gutenberg. Perfect for finding public domain works like classic literature and historical texts.
                            </p>
                            <a href="/books" class="btn btn-primary">Explore Gutenberg Books</a>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card mb-4">
                        <div class="card-body">
                            <h3 class="card-title">Open Library</h3>
                            <p class="card-text">
                                Search Open Library's comprehensive bibliographic database. Ideal for exploring works, authors, and editions with detailed publication data.
                            </p>
                            <a href="/books/openlibrary" class="btn btn-primary">Search Open Library</a>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card mb-4">
                        <div class="card-body">
                            <h3 class="card-title">Google Books</h3>
                            <p class="card-text">
                                Browse Google Books' extensive collection. Best for discovering modern books, previews, and extensive bibliographic details.
                            </p>
                            <a href="/googlebooks" class="btn btn-primary">Browse Google Books</a>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <hr>

        <!-- Display Library Items -->
        <h2>Library Collection</h2>
        <div class="row">
            <% items.forEach(item => { %>
                <div class="col-md-4">
                    <div class="card mb-4">
                        <!-- Bootstrap Carousel -->
                        <div id="carousel<%= item._id %>" class="carousel slide" data-bs-ride="carousel">
                            <div class="carousel-inner">
                                <% item.images.forEach((img, index) => { %>
                                    <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                                        <img src="<%= img %>" class="d-block w-100" alt="Item Image">
                                    </div>
                                <% }); %>
                            </div>
                            <button class="carousel-control-prev" type="button" data-bs-target="#carousel<%= item._id %>" data-bs-slide="prev">
                                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                            </button>
                            <button class="carousel-control-next" type="button" data-bs-target="#carousel<%= item._id %>" data-bs-slide="next">
                                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                            </button>
                        </div>

                        <!-- Item Details -->
                        <div class="card-body">
                            <h5 class="card-title"><%= item.name_en %> / <%= item.name_other %></h5>
                            <p class="card-text"><%= item.description_en %></p>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <%- include("footer") %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script> <!-- ✅ Bootstrap JS for carousel -->
</body>
</html>
