<% layout('layout') %> 

<div class="container">
    <h1>Book Collection</h1>
    <ul>
        <% books.forEach(book => { %>
            <li><%= book.title %> by <%= book.author %></li>
        <% }) %>
    </ul>
</div>
